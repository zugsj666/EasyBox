# EasyBox Panel API

This directory is reserved for the future NestJS API service.

No full NestJS project is initialized in the skeleton stage.

The API service will later own authentication, user management, device binding, subscription source syncing, node management, announcements, versions, and client config delivery.

