# @easybox/shared-types

Shared TypeScript types for EasyBox API contracts and package boundaries.

This package only defines types during the skeleton stage. It does not implement API clients, backend handlers, validation logic, or business rules.

